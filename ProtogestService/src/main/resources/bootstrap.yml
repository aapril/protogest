spring:
  application:
    name: protogest-service
    security.basic.enable: false
  
  datasource:
    platform: mysql
    continue-on-error: true
    initialization-mode: never
    url: jdbc:mysql://host:3306/protogest
    username: mysqlUser
    password: mysqlPass


  #jpa:
    #hibernate:
      #ddl-auto: create

server:
  connection-timeout: 60000
  port: 5000