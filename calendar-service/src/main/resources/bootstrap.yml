spring:
  application:
    name: calendar-service

  profiles:
    active: dev
    
security:
  oauth2:
    resource:
      userInfoUri: https://www.googleapis.com/oauth2/v3/userinfo
      preferTokenInfo: true
        
  datasource:
    platform: mysql
    continue-on-error: false
    initialization-mode: never
    url: jdbc:mysql://lawyerdev.coh0yeelrta5.us-east-1.rds.amazonaws.com:3306/lawyer_db
    username: root
    password: 4PROTOdb  
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/
server:
  port: 9000